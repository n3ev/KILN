node_modules/
dist/
.next/
.turbo/
out/
coverage/
*.tsbuildinfo

# Local environment. Ignore every .env variant, then re-admit the template:
# a `.env.bak` from editing credentials in place was previously untracked but
# offerable, which is one `git add -A` away from committing a live key.
.env
.env.*
!.env.example

# Zero-config local database (PGlite data directory) and dev key material
.kiln/

# Test output
playwright-report/
test-results/

# OS noise
.DS_Store
Thumbs.db
