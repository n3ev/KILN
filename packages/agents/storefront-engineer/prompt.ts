import { storefrontEngineer } from "../roster.js";

/** Fully composed Storefront Engineer system prompt, including shared safety rules. */
export const buildPrompt = storefrontEngineer.systemPrompt;
